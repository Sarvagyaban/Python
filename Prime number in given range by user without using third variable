a,b=int(input('enter starting')),int(input('enter ending'))
for n in range(a,b+1):
    
    for i in range(2,n):
        if n%i==0:
            print(n,'not a prime number')
            break
    else:
        print(n,'prime number')
